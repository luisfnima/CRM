import PlaceholderPage from '../../../components/common/PlaceholderPage';
import { Database } from 'lucide-react';

const LeadsPerList = () => {
  return (
    <PlaceholderPage 
      title="Leads por Lista"
      description="Leads por lista"
      icon={Database}
    />
  );
};

export default LeadsPerList;
