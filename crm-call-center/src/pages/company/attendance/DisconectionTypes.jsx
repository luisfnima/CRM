import PlaceholderPage from '../../../components/common/PlaceholderPage';
import { Wifi } from 'lucide-react';

const DisconectionTypes = () => {
  return (
    <PlaceholderPage 
      title="Tipos de Conexión"
      description="Configura tipos de conexión"
      icon={Wifi}
    />
  );
};

export default DisconectionTypes;
