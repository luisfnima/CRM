import PlaceholderPage from '../../../components/common/PlaceholderPage';
import { Calendar } from 'lucide-react';

const LeadsCalendar = () => {
  return (
    <PlaceholderPage 
      title="Calendarios de Leads"
      description="Vista de calendario"
      icon={Calendar}
    />
  );
};

export default LeadsCalendar;
