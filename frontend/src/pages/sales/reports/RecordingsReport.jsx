import PlaceholderPage from '../../../components/common/PlaceholderPage';
import { Mic } from 'lucide-react';

const RecordingsReport = () => {
  return (
    <PlaceholderPage 
      title="Grabaciones por Día"
      description="Audios grabados"
      icon={Mic}
    />
  );
};

export default RecordingsReport;
